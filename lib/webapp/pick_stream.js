/*
 * This script picks the appropriate file to stream based on which
 * has most recently been modified.
 *
 */

var fs = require('fs');
console.log('test');

function getRecentlyModified(path, list) {
    // fs.Stats object "mtime" = modified time
}
